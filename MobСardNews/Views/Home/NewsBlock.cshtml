@model MobСardNews.Models.News
@{
    ViewBag.Title = "News";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div>
        <h4>News</h4>
        <hr />
        <dl class="dl-horizontal">
                <dt>
                        @Html.DisplayNameFor(model => model.Text)
                </dt>

                <dd>
                        @Html.DisplayFor(model => model.Text)
                </dd>

                <dt>
                        @Html.DisplayNameFor(model => model.tag)
                </dt>

                <dd>
                        @Html.DisplayFor(model => model.tag)
                </dd>

                <dt>
                        @Html.DisplayNameFor(model => model.Game)
                </dt>

                <dd>
                        @Html.DisplayFor(model => model.Game)
                </dd>

        </dl>
</div>
<div>
        @foreach(var item)
        {
            Html.ActionLink()
        }
        @*@using (Html.BeginForm())
        {
        @foreach (var item in Model.Comennts)
        {
        @Html.ValidationSummary(true)
        <div class="addcommentbox">
                <h2> Add Comment </h2>
                @Html.TextAreaFor(model => item.Text)
                <div class="ErrorMessage">
                        @Html.ValidationMessageFor(model => item.Text)
                </div>
                <input id="addComment" type="button" onclick="" value="Add" />
        </div>
        }*@
        }
</div>
<p>
        @*     @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
                @Html.ActionLink("Back to List", "Index")*@
</p>
